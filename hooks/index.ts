/**
 * Custom React hooks
 * Per rules.md Section 4: Hooks wrap services calls and expose { data, loading, error }
 */

// Placeholder - custom hooks will be implemented as needed
// Example: useUserProfile(), useJobMatches(), etc.
export {};
